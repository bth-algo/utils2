name: Run on tag

on:
  workflow_call:
    inputs:
      kmom_paths:
        required: true
        type: string

jobs:
  on-tag:
    uses: bth-algo/utils2/.github/workflows/tester.yml@main
    with:
      kmom_paths: ${{ inputs.kmom_paths }}
    secrets: inherit